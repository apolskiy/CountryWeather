[pytest]
markers =
    countries: tests targeting the REST Countries API
    weather: tests targeting the Open-Meteo Weather API
    test_id(identifier): assigned stable test identifier, e.g. CWA_10001.
        Authored once per test and republished by conftest as both an Allure
        label and a JUnit property, so a rename never forks the test's history.
